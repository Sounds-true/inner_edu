# IP-04: Квест-движок и интеграция психологических модулей

## Смысл и цель задачи

Создать систему квестов, которая переводит 23 психологических модуля (IFS, DBT, CBT, ACT, TRIZ и др.) в игровые активности для детей. Квесты должны быть короткими (5-15 минут), понятными, встроенными в метафору Понималии и естественно вести ребенка от учебных трудностей к психологической проработке. Каждый квест содержит Reality Bridge - маленькое задание для реальной жизни, которое закрепляет навык.

## Архитектура решения

### Компоненты

```
src/game/
├── quest_engine.py              # Центральный движок квестов
├── quest.py                     # Базовый класс Quest
├── quest_step.py                # Класс QuestStep (один шаг квеста)
├── quest_validator.py           # Проверка ответов и условий
└── reality_bridge.py            # Система Reality Bridge заданий

src/modules/
├── module_registry.py           # Реестр всех 23 модулей
├── module_adapter.py            # Адаптер модуль → квест
└── psychological_modules/
    ├── __init__.py
    ├── module_01_ifs.py         # IFS: части личности
    ├── module_02_dbt_emotions.py
    ├── module_03_cbt_thoughts.py
    ├── ...
    └── module_23_decision.py

src/data/quests/
├── tower_confusion/             # Квесты для Башни Непонимания
│   ├── quest_simple_words.yaml
│   ├── quest_term_hunt.yaml
│   └── ...
├── valley_words/
├── mountain_emptiness/
├── forest_calm/
├── city_mind/
├── workshop_creator/
└── bridge_actions/

src/data/modules/
└── module_quests_map.yaml       # Какие модули используют какие квесты
```

### Структура квеста (YAML)

```yaml
id: tower_quest_01_simple_words
title: "Три простых объяснения"
location: tower_confusion
psychological_module: module_15_metacognition  # Мета-познание
difficulty: easy
estimated_time_minutes: 10
target_learning_profile:
  understanding_meaning: high    # Помогает с пониманием смысла
  memory: low
  attention: low
  motivation: medium

description: |
  В Башне Непонимания живут слова, которые потеряли простые объяснения.
  Выбери одно сложное слово из учебника и найди для него три разных способа объяснить.

steps:
  - id: step_1_choose_word
    type: input_text
    prompt: "Напиши одно сложное слово, которое тебе встречается в учебниках, но непонятно."
    validation:
      min_length: 2
      max_length: 50
    hint: "Например: 'синтез', 'экосистема', 'метафора'"

  - id: step_2_explain_1
    type: choice
    prompt: "Как бы ты объяснил это слово маленькому ребёнку?"
    options:
      - text: "Показать картинку или пример"
        score: 1
        feedback: "Отлично! Визуальный образ помогает понять."
      - text: "Дать определение из словаря"
        score: 0.5
        feedback: "Неплохо, но давай попробуем проще."
      - text: "Сказать 'это когда...'"
        score: 1
        feedback: "Круто! 'Это когда' - отличный способ."

  - id: step_3_explain_2
    type: input_text
    prompt: "Теперь объясни это слово своими словами, без сложных терминов."
    validation:
      min_length: 10
      max_length: 200
    hint: "Попробуй использовать простые слова, которые знает любой человек."

  - id: step_4_real_example
    type: input_text
    prompt: "Придумай пример из жизни, где встречается это понятие."
    validation:
      min_length: 10
      max_length: 200

  - id: step_5_reflection
    type: choice
    prompt: "Стало ли тебе понятнее после того, как ты сам объяснил?"
    options:
      - text: "Да, теперь ясно!"
        score: 1
      - text: "Немного яснее"
        score: 0.7
      - text: "Всё ещё непонятно"
        score: 0.3

completion_message: |
  🎉 Отлично! Ты только что применил один из самых мощных способов понимания:
  **объяснить самому себе**. Когда мы учим других (даже воображаемых),
  наш мозг лучше усваивает материал. Это называется "эффект протеже".

rewards:
  learning_profile:
    understanding_meaning: +2     # Улучшение профиля
  experience_points: 10
  location_progress:
    tower_confusion: +1

reality_bridge:
  id: rb_tower_01
  title: "Объясни одно слово учителю или однокласснику"
  description: |
    Завтра на уроке попробуй объяснить одно слово (любое) своему однокласснику
    или попроси учителя объяснить тебе что-то непонятное.
    Это не стыдно - это смелость!
  deadline_hours: 48
  reminder_hours: 24
  verification:
    type: self_report
    prompt: "Получилось выполнить? Как прошло?"
    options:
      - "Да, объяснил однокласснику"
      - "Да, спросил у учителя"
      - "Нет, не решился"
      - "Не было возможности"

psychological_insights:
  - module: module_15_metacognition
    technique: "Feynman Technique"
    explanation: |
      Техника Фейнмана: если можешь объяснить простыми словами - значит понял.
      Если нет - найди пробел в понимании и заполни его.

  - module: module_01_ifs
    technique: "Parts Work"
    explanation: |
      IFS: Критикующая часть может говорить "я глупый, не понимаю".
      Но есть и Любопытная часть, которая хочет разобраться.
      Давай слушать вторую, а не первую.
```

### Структура модуля (Python)

```python
# src/modules/psychological_modules/module_15_metacognition.py

class MetacognitionModule:
    """
    Модуль 15: Метапознание (учись учиться)

    Психологическая основа: метакогнитивные стратегии, Feynman Technique,
    активное вспоминание, распределенное повторение.
    """

    id = "module_15_metacognition"
    name = "Метапознание"
    description = "Учись учиться: техники, которые помогают понимать, как ты учишься"

    core_techniques = [
        "feynman_technique",        # Объясни простыми словами
        "active_recall",            # Активное вспоминание
        "spaced_repetition",        # Распределенное повторение
        "elaborative_interrogation" # Углубленные вопросы "почему?"
    ]

    target_difficulties = [
        "understanding_meaning",    # Не понимаю смысл
        "memory"                    # Не могу запомнить
    ]

    def get_quests(self) -> list[str]:
        """Возвращает ID квестов, связанных с этим модулем"""
        return [
            "tower_quest_01_simple_words",
            "tower_quest_02_teach_younger",
            "city_quest_03_active_recall"
        ]

    def get_techniques_for_context(self, context: dict) -> list[dict]:
        """
        Возвращает рекомендованные техники в зависимости от контекста
        context = {
            'learning_profile': {...},
            'subject': 'math',
            'emotion': 'frustration'
        }
        """
        pass

    def get_mini_insight(self, quest_result: dict) -> str:
        """
        Генерирует короткий психологический инсайт после квеста
        """
        pass
```

## Полный flow работы функционала

### 1. Выбор квеста

**Вариант А: Квест рекомендован системой**

1. Ребенок находится в локации Tower of Confusion
2. Location.get_available_activities() вызывается
3. QuestEngine.get_available_quests(location="tower_confusion", user_id=123)
4. Фильтрация:
   - Квесты, доступные в этой локации
   - Еще не выполненные
   - Соответствующие learning profile (если understanding_meaning низкое - приоритет таким квестам)
5. Возвращается список из 3-5 квестов
6. Бот показывает: "Вот что можно сделать здесь:" + список с краткими описаниями
7. Ребенок выбирает квест кнопкой

**Вариант Б: Квест предложен на основе модуля**

1. Система определила, что ребенок испытывает трудность X (например, "не понимаю смысл")
2. ModuleRegistry.get_module_for_difficulty("understanding_meaning") → module_15_metacognition
3. MetacognitionModule.get_quests() → ["tower_quest_01_simple_words", ...]
4. QuestEngine рекомендует первый из доступных
5. Бот: "Попробуй квест 'Три простых объяснения' - он поможет с пониманием сложных слов"

### 2. Прохождение квеста (step by step)

1. QuestEngine.start_quest(user_id=123, quest_id="tower_quest_01_simple_words")
2. Загружается YAML квеста
3. Создается QuestSession:
   ```json
   {
     "session_id": "session_123_456",
     "user_id": 123,
     "quest_id": "tower_quest_01_simple_words",
     "started_at": "2025-11-07T15:30:00Z",
     "current_step": "step_1_choose_word",
     "steps_completed": [],
     "answers": {},
     "status": "in_progress"
   }
   ```
4. Показывается первый шаг: prompt из step_1_choose_word
5. Ребенок вводит ответ (например, "экосистема")
6. QuestValidator.validate_answer(step_id, answer) проверяет:
   - Тип (текст, выбор, число)
   - Длина (min_length, max_length)
   - Формат (если нужно)
7. Если валидация OK:
   - Ответ сохраняется в answers
   - current_step переходит на step_2_explain_1
   - Показывается следующий prompt
8. Если валидация FAIL:
   - Показывается hint
   - Ребенок пробует еще раз (макс. 3 попытки)
9. Процесс повторяется для всех шагов

### 3. Завершение квеста

1. Последний шаг пройден (step_5_reflection)
2. QuestEngine.complete_quest(session_id)
3. Расчет результатов:
   - Сумма score из всех choice-шагов
   - Качество ответов (длина текста, использование hint)
   - Время прохождения
4. Обновление user profile:
   - learning_profile: understanding_meaning += 2
   - experience_points += 10
   - location_progress.tower_confusion += 1
   - completed_quests.append("tower_quest_01_simple_words")
5. Показывается completion_message с эмодзи
6. Психологический инсайт:
   ```
   💡 Интересный факт!
   Техника, которую ты только что использовал, называется "Метод Фейнмана".
   Её придумал знаменитый физик. Суть простая: если можешь объяснить -
   значит понял. Используй её перед каждой контрольной!
   ```
7. Reality Bridge предлагается:
   ```
   🌉 Мост в реальность
   Попробуй завтра на уроке объяснить одно слово однокласснику.
   Это закрепит твой навык!
   [Принять задание] [Пропустить]
   ```

### 4. Reality Bridge flow

1. Если ребенок принимает:
   - RealityBridge.create_task(user_id, rb_id="rb_tower_01", deadline=48h)
   - Задание сохраняется в user profile:
     ```json
     {
       "reality_bridges": {
         "active": [
           {
             "id": "rb_tower_01",
             "quest_id": "tower_quest_01_simple_words",
             "created_at": "2025-11-07T15:45:00Z",
             "deadline": "2025-11-09T15:45:00Z",
             "status": "active"
           }
         ]
       }
     }
     ```
2. Через 24 часа (reminder):
   - Бот отправляет напоминание: "Привет! Не забыл про задание 'Объяснить слово'? 😊"
3. Через 48 часов (deadline) или раньше:
   - Бот спрашивает: "Получилось выполнить задание?"
   - Варианты ответа (из verification.options)
4. Ребенок отвечает:
   - "Да, объяснил однокласснику" → статус "completed", +5 XP, лог успеха
   - "Нет, не решился" → статус "failed", мягкая поддержка + предложение попробовать легче задание
5. Статистика Reality Bridge используется для адаптации сложности будущих заданий

## API и интерфейсы

### QuestEngine

**get_available_quests(user_id: int, location: str = None, limit: int = 5) -> list[Quest]**
- Возвращает доступные квесты
- Фильтрация по локации (если указана)
- Фильтрация по не выполненным
- Сортировка по relevance (learning profile + emotional state)

**start_quest(user_id: int, quest_id: str) -> QuestSession**
- Создает новую сессию квеста
- Загружает первый шаг
- Сохраняет в JSON
- Возвращает объект сессии

**get_current_step(session_id: str) -> QuestStep**
- Возвращает текущий шаг квеста
- Prompt, тип ответа, validation rules

**submit_answer(session_id: str, answer: any) -> dict**
- Проверяет ответ через QuestValidator
- Если OK - переход на следующий шаг
- Если FAIL - возврат с hint
- Возвращает: {"valid": bool, "feedback": str, "next_step": QuestStep or None}

**complete_quest(session_id: str) -> dict**
- Финализация квеста
- Расчет наград
- Обновление user profile
- Генерация psychological_insight
- Возвращает: {"completion_message": str, "rewards": dict, "reality_bridge": dict}

**abandon_quest(session_id: str) -> None**
- Ребенок хочет выйти из квеста
- Статус "abandoned"
- Мягкая реакция: "Ничего страшного, можешь вернуться позже"

### Quest (класс)

**load_from_yaml(yaml_path: str) -> Quest**
- Парсинг YAML → объект Quest

**get_step(step_id: str) -> QuestStep**
- Возвращает шаг по ID

**calculate_rewards(answers: dict) -> dict**
- Расчет наград на основе ответов
- learning_profile изменения
- XP
- location_progress

**get_psychological_insight(answers: dict) -> str**
- Генерация инсайта на основе модуля и ответов

### ModuleRegistry

**get_all_modules() -> list[PsychologicalModule]**
- Возвращает все 23 модуля

**get_module_by_id(module_id: str) -> PsychologicalModule**
- Получить модуль по ID

**get_modules_for_difficulty(difficulty_type: str) -> list[PsychologicalModule]**
- Какие модули помогают с этой трудностью
- difficulty_type: "understanding_meaning", "memory", "attention", "motivation"

**get_quests_for_module(module_id: str) -> list[str]**
- Возвращает список quest_id, связанных с модулем

### RealityBridge

**create_task(user_id: int, rb_id: str, deadline_hours: int) -> dict**
- Создает Reality Bridge задание
- Сохраняет в user profile
- Устанавливает таймеры напоминаний

**get_active_tasks(user_id: int) -> list[dict]**
- Возвращает активные Reality Bridge задания

**send_reminder(task_id: str) -> None**
- Отправляет напоминание через бота

**complete_task(task_id: str, user_response: str) -> None**
- Завершает задание
- Обновляет статус
- Начисляет награды (если успех)

**get_completion_rate(user_id: int) -> float**
- Процент выполненных Reality Bridge заданий
- Используется для адаптации сложности

## Взаимодействие компонентов

```
User → Bot Handler
         ↓
    QuestEngine.get_available_quests()
         ↓
    LocationManager (фильтр по локации)
         ↓
    LearningProfile (фильтр по profile)
         ↓
    Quest YAML loaded
         ↓
    User выбирает квест
         ↓
    QuestEngine.start_quest() → QuestSession
         ↓
    Loop: get_current_step() → User answer → submit_answer()
         ↓
    QuestValidator.validate()
         ↓
    If all steps done → complete_quest()
         ↓
    ModuleRegistry → PsychologicalInsight
         ↓
    RealityBridge.create_task()
         ↓
    Update UserProfile (JSON)
```

## Порядок реализации

### Этап 1: Базовая структура (3-4 часа)

1. Создать Quest, QuestStep, QuestSession классы
2. Создать QuestEngine с методами start_quest, get_current_step
3. YAML-парсер для одного квеста
4. Написать 1 тестовый квест (tower_quest_01_simple_words.yaml)
5. Простой flow: начать → пройти 2 шага → завершить

### Этап 2: Валидация и типы ответов (2-3 часа)

1. QuestValidator с правилами (min_length, max_length, regex)
2. Типы шагов: input_text, choice, multiple_choice
3. Обработка hint при ошибке
4. Лимит попыток (3 попытки, потом skip или abandon)

### Этап 3: Интеграция с модулями (3-4 часа)

1. Создать ModuleRegistry
2. Реализовать 3 модуля как примеры:
   - module_15_metacognition
   - module_02_dbt_emotions
   - module_03_cbt_thoughts
3. Связать квесты с модулями через module_quests_map.yaml
4. Генерация psychological_insight после квеста

### Этап 4: Reality Bridge (2-3 часа)

1. RealityBridge класс с методами create_task, get_active_tasks
2. Таймеры напоминаний (через APScheduler или аналог)
3. Верификация выполнения (self-report через кнопки)
4. Статистика completion_rate

### Этап 5: Награды и прогресс (2 часа)

1. Расчет наград: XP, learning_profile изменения
2. Обновление user profile после квеста
3. Логика разблокировки новых квестов (prerequisites)
4. Уведомление о разблокировке

### Этап 6: Создание контента (6-8 часов)

1. Написать 10-15 квестов для разных локаций
2. Распределить квесты по психологическим модулям
3. Создать Reality Bridge задания для каждого квеста
4. Написать psychological_insights для каждого модуля

## Критичные граничные случаи

### 1. Ребенок бросает квест на середине

- QuestEngine.abandon_quest() вызывается
- Статус "abandoned", сохраняется прогресс
- Мягкая реакция: "Ничего страшного! Можешь вернуться, когда захочешь."
- Квест остается в списке доступных
- Если ребенок начинает снова - можно предложить продолжить с того места

### 2. Ответ не проходит валидацию 3 раза

- После 3 неудачных попыток - показать skip button
- "Не получается? Можешь пропустить этот шаг или вернуться позже."
- Если skip - шаг помечается как "skipped", квест продолжается
- Но reward уменьшается (меньше XP, меньше learning_profile бонус)

### 3. Reality Bridge не выполнен в срок

- Статус "expired"
- НЕ показывается как неудача, а как "не получилось в этот раз"
- Бот: "Ничего страшного, что не успел. Попробуем другое задание?"
- Статистика учитывается, но без негативной реакции
- Следующее Reality Bridge будет проще

### 4. Ребенок пытается "читерить" (быстрые бессмысленные ответы)

- Валидация min_length помогает
- Если ответ слишком короткий или бессмысленный (например, "ааааа"):
  - Hint: "Попробуй чуть подробнее, хотя бы пару слов"
  - Не блокировать, но и не давать полные награды
- QuestEngine может снизить reward, если качество ответов низкое

### 5. Квест содержит триггерный контент для ребенка

- В YAML можно добавить content_warning:
  ```yaml
  content_warning:
    themes: ["сложные чувства", "школьный стресс"]
    skip_available: true
  ```
- Перед началом квеста показать: "Этот квест про сложные чувства. Если не хочешь - можешь выбрать другой."
- Кнопка "Пропустить" всегда доступна

## Объем работ

### Входит в реализацию

- QuestEngine с CRUD операциями для квестов
- Quest, QuestStep, QuestSession классы
- QuestValidator с типами input_text, choice, multiple_choice
- ModuleRegistry с 3 примерами модулей (для MVP)
- RealityBridge система с напоминаниями и верификацией
- 10-15 квестов в YAML для разных локаций
- Связь квестов с психологическими модулями
- Psychological insights после каждого квеста
- Система наград (XP, learning_profile, location_progress)
- Команды для бота: /quests, /start_quest <id>, /abandon_quest

### Не входит в MVP

- Все 23 модуля полностью проработаны (только 3 для примера)
- Динамическая генерация квестов (на основе AI)
- Мультиплеерные квесты (совместное прохождение)
- Квесты с таймером (ограничение по времени)
- Квесты с визуальными головоломками (пока только текст)
- Адаптивная сложность квестов (A/B тест для будущего)
- Система достижений и бейджей (отдельная фича)

## Допущения

- **Допущение 1**: Дети 7-14 лет могут печатать текстовые ответы (10-200 символов). Если нет - добавить голосовой ввод позже.
- **Допущение 2**: YAML достаточно гибкий для описания квестов. Если понадобится сложная логика (ветвления) - можно добавить DSL.
- **Допущение 3**: Self-report верификация Reality Bridge достаточна для MVP. Позже можно добавить объективные метрики (фото, GPS, и т.д.).
- **Допущение 4**: 10-15 квестов достаточно для валидации концепции. После MVP можно масштабировать до 50-100.
- **Допущение 5**: Психологические инсайты статичные (из YAML). В будущем можно сделать динамическими через AI.

## Открытые вопросы

1. **Сколько квестов должно быть доступно одновременно?** 3-5 или все сразу? (Риск: overwhelm vs ограничение выбора)
2. **Должны ли квесты иметь prerequisites (зависимости)?** Например, квест 2 открывается только после квеста 1? (Для MVP - нет)
3. **Как часто обновлять контент квестов?** Статичные YAML или загрузка с сервера? (Для MVP - статичные)
4. **Reality Bridge обязательны или опциональны?** Можно ли закончить квест без принятия задания? (Да, опциональны)

## Acceptance criteria

### Базовая функциональность

- [ ] Пользователь может увидеть список доступных квестов в локации
- [ ] Пользователь может начать квест и пройти все шаги
- [ ] Каждый шаг квеста показывает prompt и принимает ответ
- [ ] Валидация работает (min_length, max_length, тип ответа)
- [ ] После 3 неудачных попыток появляется кнопка skip
- [ ] После завершения квеста показывается completion_message
- [ ] Начисляются награды (XP, learning_profile изменения)
- [ ] Psychological insight показывается после квеста

### Reality Bridge

- [ ] После квеста предлагается Reality Bridge задание
- [ ] Пользователь может принять или пропустить задание
- [ ] Если принято - задание сохраняется в user profile
- [ ] Через 24 часа приходит напоминание
- [ ] Через 48 часов бот спрашивает о выполнении
- [ ] Статистика completion_rate обновляется

### Интеграция с модулями

- [ ] ModuleRegistry содержит 3 примера модулей
- [ ] Квесты связаны с модулями через module_quests_map.yaml
- [ ] После квеста показывается инсайт из соответствующего модуля
- [ ] Можно получить квесты для конкретной трудности (understanding_meaning → metacognition quests)

### Граничные случаи

- [ ] Можно бросить квест на середине без негативных последствий
- [ ] При повторном запуске квеста можно продолжить с того места
- [ ] Если ответ не проходит валидацию - показывается hint
- [ ] Если Reality Bridge не выполнен в срок - статус "expired" без наказания

### Ручной сценарий проверки

1. Войти в Tower of Confusion
2. Увидеть список из 3-5 доступных квестов
3. Начать квест "Три простых объяснения"
4. Пройти step_1: ввести слово "экосистема"
5. Пройти step_2: выбрать вариант "Это когда..."
6. Пройти step_3: написать объяснение (50+ символов)
7. Пройти step_4: придумать пример из жизни
8. Пройти step_5: выбрать "Да, теперь ясно!"
9. Увидеть completion_message с эмодзи
10. Увидеть psychological insight про Feynman Technique
11. Принять Reality Bridge задание
12. Через 24 часа получить напоминание
13. Через 48 часов ответить "Да, объяснил однокласснику"
14. Увидеть +5 XP и статус "completed"

## Definition of Done

- [ ] QuestEngine, Quest, QuestSession, QuestStep классы реализованы
- [ ] QuestValidator работает для всех типов ответов
- [ ] ModuleRegistry с 3 примерами модулей создан
- [ ] RealityBridge система с таймерами и верификацией работает
- [ ] 10-15 квестов в YAML созданы и протестированы
- [ ] module_quests_map.yaml связывает квесты и модули
- [ ] Psychological insights добавлены для всех квестов
- [ ] Команды /quests, /start_quest, /abandon_quest работают в боте
- [ ] Логи для всех ключевых событий (start, complete, abandon, skip)
- [ ] Краткий README в docs/features/quest_engine.md создан

## Минимальные NFR для MVP

### Производительность

- Загрузка квеста из YAML: <50ms
- Валидация ответа: <20ms
- Расчет наград: <30ms
- Генерация psychological insight: <50ms
- Одновременных активных сессий: до 50 пользователей

### Надежность

- Если YAML квеста поврежден → fallback на generic quest + лог ошибки
- Если таймер Reality Bridge не сработал → повторная попытка через 1 час
- Если расчет наград упал → дать минимальные награды (XP=5) + лог
- Сессия квеста сохраняется после каждого шага (защита от потери прогресса)

### Ограничения по ресурсам

- YAML файл квеста: до 15KB
- Активных сессий на пользователя: максимум 3 одновременно
- История завершенных квестов: хранить последние 100 (потом архивировать)
- Reality Bridge активных заданий: максимум 5 на пользователя

## Требования безопасности

- User input валидируется (длина, тип, XSS защита)
- YAML файлы только для чтения (не пишутся динамически)
- Reality Bridge не требует фото/GPS (для MVP - только self-report)
- Логи не содержат полные ответы детей (только метрики: длина, тип)
- PII не логируется (только user_id)

## Наблюдаемость

### Логи

- INFO: "User {user_id} started quest {quest_id}"
- INFO: "User {user_id} completed quest {quest_id} in {duration} seconds"
- INFO: "User {user_id} abandoned quest {quest_id} at step {step_id}"
- INFO: "Reality Bridge task {task_id} created for user {user_id}"
- INFO: "Reality Bridge task {task_id} completed: {user_response}"
- ERROR: "Failed to load quest {quest_id} from YAML: {error}"
- WARNING: "User {user_id} failed validation {count} times at step {step_id}"

### Метрики

- Счетчик запусков квестов (по quest_id)
- Счетчик завершений vs abandons (completion rate)
- Среднее время прохождения квеста
- Reality Bridge completion rate (процент выполненных)
- Топ-5 самых популярных квестов
- Топ-5 квестов с наибольшим abandon rate

### Трассировка

- Span: quest_execution (от start до complete)
- Span: step_validation (от submit до result)
- Span: reward_calculation (от complete до update profile)

## Релиз

- Включение через feature flag: `features.quest_engine.enabled`
- Если флаг выключен → бот работает без квестов (только мини-игры и диалоги)
- План развертывания:
  1. Тестовое окружение: 5 тестовых пользователей, 3 квеста
  2. Staging: 20% пользователей, 10 квестов, мониторинг 48 часов
  3. Production: 50% пользователей, 15 квестов, мониторинг 1 неделя
  4. Production: 100% пользователей

## Откат

### Условия отката

- Quest completion rate <20% (дети не завершают квесты)
- Abandon rate >70% (почти все бросают)
- Ошибки загрузки YAML >10%
- Reality Bridge не работают (таймеры не срабатывают)

### Шаги отката

1. Выключить feature flag: `features.quest_engine.enabled = false`
2. Бот возвращается к режиму без квестов
3. Активные сессии сохраняются (можно будет возобновить после фикса)
4. Reality Bridge задания остаются активными (не отменяются)
5. Анализ логов и метрик для выявления проблемы
6. После исправления - повторный релиз с 20% пользователей

## Риски и митигации

### Риск 1: Квесты слишком сложные или скучные для детей

**Митигация**:
- Протестировать 3 квеста с фокус-группой детей 7-14 лет
- Собрать фидбек: "Было интересно?" (лайк/дизлайк)
- A/B тест длины квестов (5 шагов vs 3 шага)
- Адаптировать язык и сложность на основе feedback

### Риск 2: Дети не понимают психологические инсайты

**Митигация**:
- Упростить язык (без терминов типа "метакогнитивный")
- Использовать метафоры и примеры из жизни
- Сделать инсайты опциональными (кнопка "Узнать больше")
- Тестировать readability (должен быть понятен 10-летнему)

### Риск 3: Reality Bridge задания не выполняются (completion rate <10%)

**Митигация**:
- Сделать задания максимально простыми (1 действие, 2 минуты)
- Опциональность (не обязательно для прогресса)
- Адаптивная сложность (если не выполняют - предлагать легче)
- Мотивация: бейдж "Мост-строитель" за 5 выполненных Reality Bridge

### Риск 4: YAML файлы станут слишком большими и сложными

**Митигация**:
- Лимит 15KB на квест
- Если логика сложная - вынести в Python (Quest.custom_logic())
- Периодический review размера и читабельности YAML

### Риск 5: Дети "читерят" (случайные ответы для быстрого прохождения)

**Митигация**:
- Валидация не только формальная (длина), но и смысловая (ключевые слова)
- Уменьшать reward, если качество ответов низкое
- Не блокировать, но давать меньше XP
- Квесты должны быть короткими (5-10 минут), чтобы не было соблазна читерить

## Параметры стека

- **Язык**: Python 3.11+
- **Фреймворк**: python-telegram-bot 20.x
- **Парсинг YAML**: PyYAML 6.0+
- **Таймеры**: APScheduler 3.10+ (для Reality Bridge напоминаний)
- **Хранилище**: JSON-файлы (для MVP), планируется миграция на SQLite
- **Деплой**: Docker-контейнер на виртуальной машине

## Самопроверка плана перед выдачей

- [x] Нет кода и псевдокода (только структура YAML и описание логики)
- [x] Заполнены scope, acceptance, risk, release
- [x] Формат файла: IP-04-quest-engine-modules.md
- [x] Нет упоминаний секретов и приватных URL
- [x] Все пути в POSIX-формате

## Проверки перед "ломкой" API

- [x] Подтверждено отсутствие внешних потребителей API (MVP, нет внешних сервисов)
- [x] Нет зависимых задач в текущем спринте

## Профиль языка

- primary_language: python
- stdlib_preferred: true
- external_libs: PyYAML (квесты), APScheduler (таймеры Reality Bridge), python-telegram-bot (UI)
