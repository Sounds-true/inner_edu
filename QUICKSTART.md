# InnerWorld Edu - Быстрый старт 🚀

## ✅ Что реализовано

1. **AI Quest Builder** - создание квестов через чат с GPT-4
2. **YAML → Graph конвертер** - превращает существующие квесты в майнд-карты
3. **Quest Library** - библиотека квестов с визуальным редактором
4. **React Flow** - node-based интерфейс для редактирования

---

## 🎯 Главная фича: Визуализация квестов Понималии

Можешь загрузить существующий квест "Три простых объяснения" и увидеть его как граф:

```
[Start] → [Выбери слово] → [Способ объяснения] → [Свои слова] → [Пример] → [Рефлексия] → [Reality Bridge] → [End]
```

---

## 📦 Установка

### 1. Backend (FastAPI)

```bash
cd /home/user/inner_edu/backend

# Установить зависимости
pip install -r requirements.txt

# Создать .env
cat > .env << EOF
OPENAI_API_KEY=your_key_here
DATABASE_URL=postgresql+asyncpg://postgres:password@localhost/innerworld_edu
EOF
```

### 2. Frontend (React)

```bash
cd /home/user/inner_edu/frontend

# Установить зависимости
npm install
```

### 3. PostgreSQL

```bash
# Создать базу
createdb innerworld_edu

# Или через psql
psql -U postgres
CREATE DATABASE innerworld_edu;
\q
```

---

## 🚀 Запуск

### Terminal 1: Backend

```bash
cd /home/user/inner_edu/backend
python main.py

# Должен запуститься на http://localhost:8000
# Swagger: http://localhost:8000/docs
```

### Terminal 2: Frontend

```bash
cd /home/user/inner_edu/frontend
npm run dev

# Должен запуститься на http://localhost:5173
```

---

## 🎮 Тестирование

### Сценарий 1: Загрузить существующий квест из YAML

1. Открой **http://localhost:5173**
2. Нажми **📚** (иконка книги в правом верхнем углу чата)
3. Нажми **"Загрузить квесты из YAML"**
   - Система конвертирует `/src/data/quests/**/*.yaml` в графы
   - Загрузит в БД
4. Появится список квестов
5. Кликни на **"Три простых объяснения"**
6. **Увидишь граф квеста!** 🎉

### Сценарий 2: Создать новый квест через AI

1. В чате напиши: **"Хочу квест про фотосинтез"**
2. AI спросит:
   - Возраст ребенка? → **"8 лет"**
   - Какие сложности? → **"Плохо запоминает"**
   - Сколько шагов? → **"5"**
3. AI сгенерирует граф квеста
4. Увидишь майнд-карту с узлами

### Сценарий 3: Редактировать квест визуально

1. Загрузи квест из библиотеки
2. Кликни на узел (TODO: пока не работает редактирование)
3. Попроси AI: **"Сделай второй шаг проще"** (TODO)
4. AI обновит узел

---

## 🔍 Что смотреть

### В фронтенде:

- **Левая панель** - чат с AI
  - 📚 кнопка открывает библиотеку
  - Показывает стадию диалога
  - Показывает название загруженного квеста

- **Центр** - React Flow граф
  - **🟢 Зеленый круг** - Start (начало)
  - **🔵 Синие блоки** - QuestStep (шаги квеста)
  - **🟡 Желтые ромбы** - Choice (выборы)
  - **🟣 Фиолетовые шестиугольники** - Reality Bridge
  - **🔴 Красный круг** - End (конец)

- **Модальное окно библиотеки:**
  - Список квестов с метаданными
  - Фильтры по сложности, локации
  - Рейтинг и количество прохождений

### В бэкенде:

- **Swagger UI**: http://localhost:8000/docs
  - `/api/builder/chat` - чат с AI
  - `/api/builder/generate_graph` - генерация графа
  - `/api/quests/existing` - список квестов
  - `/api/quests/load_yaml_quests` - загрузить YAML квесты

---

## 📊 Структура существующего квеста

Квест **"Три простых объяснения"** конвертируется в:

```json
{
  "nodes": [
    {"id": "start", "type": "start", "data": {"title": "Три простых объяснения"}},
    {"id": "step_1_choose_word", "type": "questStep", ...},
    {"id": "step_2_explain_method", "type": "choice", ...},
    {"id": "step_3_own_words", "type": "questStep", ...},
    {"id": "step_4_real_example", "type": "questStep", ...},
    {"id": "step_5_reflection", "type": "choice", ...},
    {"id": "reality_bridge", "type": "realityBridge", ...},
    {"id": "end", "type": "end", ...}
  ],
  "edges": [
    {"source": "start", "target": "step_1_choose_word"},
    {"source": "step_1_choose_word", "target": "step_2_explain_method"},
    ...
  ]
}
```

Визуально:

```
         [Start: Три простых объяснения]
                      ↓
         [Шаг 1: Выбери сложное слово]
                      ↓
   [Выбор: Как объяснишь маленькому ребенку?]
                      ↓
      [Шаг 3: Объясни своими словами]
                      ↓
       [Шаг 4: Придумай пример из жизни]
                      ↓
    [Выбор: Стало понятнее после объяснения?]
                      ↓
  [Reality Bridge: Объясни слово учителю или однокласснику]
                      ↓
                   [End]
```

---

## 🐛 Troubleshooting

### Backend не запускается
```bash
# Проверь что PostgreSQL запущен
pg_isready

# Проверь переменные окружения
cat backend/.env

# Проверь что порт 8000 свободен
lsof -i :8000
```

### Frontend не загружает квесты
```bash
# Проверь что backend запущен
curl http://localhost:8000/health

# Проверь CORS в browser console (F12)
```

### "No YAML quests found"
```bash
# Проверь что YAML квесты существуют
ls -la /home/user/inner_edu/src/data/quests/tower_confusion/

# Должен быть файл:
# quest_01_simple_words.yaml
```

---

## 🎉 Следующие шаги

После запуска и тестирования:

1. ✅ **Попробуй создать квест через AI** - пообщайся с GPT-4
2. ✅ **Загрузи квест из библиотеки** - кликни 📚
3. ✅ **Посмотри на граф** - увидь структуру квеста как майнд-карту
4. ⏳ **Редактируй узлы** (TODO: нужно допилить NodeEditor)
5. ⏳ **Сохрани квест** (TODO: нужно добавить кнопку сохранения)
6. ⏳ **Запусти квест ребенком** (TODO: интеграция с StateManager)

---

## 📝 Commit & Push

Код готов! Запушь изменения:

```bash
cd /home/user/inner_edu

# Уже закоммитили локально
git log --oneline -2

# Пуш (требует твою аутентификацию)
git push -u origin claude/fork-repo-architecture-011CUtYG7BkBAjCcGNJrC3Ef
```

Или смёржь в main:

```bash
git checkout main
git merge claude/fork-repo-architecture-011CUtYG7BkBAjCcGNJrC3Ef
git push origin main
```

---

Удачи! 🚀✨
